{
  "private": true,
  "scripts": {
      "build": "pulp build --to dist/app.js",
      "psci": "pulp psci",
      "watch": "pulp -w build --to dist/app.js",
      "test": "pulp test"
  },
  "devDependencies": {
    "pulp": "^11.0.0",
    "purescript": "^0.11.4",
    "purescript-psa": "^0.5.0"
  }
}
